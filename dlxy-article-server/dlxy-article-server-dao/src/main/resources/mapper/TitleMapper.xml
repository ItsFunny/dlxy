<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.dlxy.server.article.dao.mybatis.TitleMybatisDao">
	<select id="countTilte" parameterType="java.util.Map">
		select count(1) from dlxy_title  where 1=1
		<if test="null!=parentTitleId" > and title_parent_id=#{parentTitleId}</if>	
	</select>
</mapper>