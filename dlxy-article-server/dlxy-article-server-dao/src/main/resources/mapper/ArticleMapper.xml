<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.dlxy.server.article.dao.mybatis.ArticleDao">
	
	<update id="updateInBatch" parameterType="java.util.Map">
		update dlxy_article set article_status=#{status}
		where article_id in 
		<foreach collection="ids" index="index" open="(" separator="," close=")" item="id">
			#{id}
		</foreach>
	</update>
	
</mapper>